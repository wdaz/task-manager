import { UserFuleNamePipe } from './user-fule-name.pipe';

describe('UserFuleNamePipe', () => {
  it('create an instance', () => {
    const pipe = new UserFuleNamePipe();
    expect(pipe).toBeTruthy();
  });
});
